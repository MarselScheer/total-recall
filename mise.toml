[settings]
minimum_release_age = "30d"

[tools]
node = "latest"
"npm:@fission-ai/openspec" = "1.11.0"

[tasks.test]
description = "Run ERT unit tests"
run = "emacs --batch -L . -L tests $(for f in tests/test-*.el; do echo -n ' -l '${f%.el}; done) -f ert-run-tests-batch-and-exit"
